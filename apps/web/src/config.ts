// ============================================================
// config.ts — Web 应用默认配置常量
// ============================================================

/**
 * 编辑器自动保存防抖延迟（毫秒）
 * 用户停止输入后等待该时间再触发保存，避免频繁写入
 */
export const AUTO_SAVE_DELAY_MS = 1000;
